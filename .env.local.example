# Supabase — safe to expose (anon key, RLS enforced)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase — server only, never prefix with NEXT_PUBLIC_
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Admin access — server only, comma-separated, no spaces
ADMIN_ALLOWED_EMAILS=m.varghese@northeastern.edu,sharma.asm@northeastern.edu,asmitasharma.nu@gmail.com

# AI enrichment — server only
ANTHROPIC_API_KEY=your-anthropic-api-key
